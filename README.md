# HealthTrack-CSCI4710-Project

## Required Commands

Run the following command inside the **Backend** folder:

```bash
npm install
```
To start the app run the following command inside the **Backend** folder:

```bash
npm run dev
```